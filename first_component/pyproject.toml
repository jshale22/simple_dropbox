[project]
name = "dropbox_example"
version = "0.1.0"
description = "A Dropbox Example"
readme = "README.md"
requires-python = ">=3.12.3"
dependencies = [
    "fastapi",
    "uvicorn"
]

[tool.uv]
dev-dependencies = [
    "pytest",
    "httpx"
]